{
  "name": "@event-scout/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "pnpm build && node dist/index.js",
    "start": "node dist/index.js",
    "typecheck": "tsc -b",
    "test": "pnpm build && node --test dist/views/*.test.js"
  },
  "dependencies": {
    "@event-scout/db": "workspace:*",
    "@event-scout/shared": "workspace:*"
  }
}
